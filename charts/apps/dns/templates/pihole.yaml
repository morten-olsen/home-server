apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-{{ .Release.Name }}-pihole-data
  labels:
    storage.k8s.io/name: nfs
    storage.k8s.io/part-of: kubernetes-complete-reference
    storage.k8s.io/created-by: ssbostan
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-client
  resources:
    requests:
      storage: 1Gi

---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: "{{ .Release.Name }}-pihole-deployment"
spec:
  selector:
    matchLabels:
      app: "{{ .Release.Name }}-pihole-pod"
  replicas: 1
  template:
    metadata:
      labels:
        app: "{{ .Release.Name }}-pihole-pod"
    spec:
      containers:
        - image: pihole/pihole:latest
          imagePullPolicy: Always
          name: "{{ .Release.Name }}-pihole"
          ports:
            - containerPort: 80
            - containerPort: 53
          volumeMounts:
            - mountPath: /app/data
              name: data 
      volumes:
        - name: data 
          persistentVolumeClaim:
            claimName: "pvc-{{ .Release.Name }}-pihole-data"

---

apiVersion: v1
kind: Service
metadata:
  name: "{{ .Release.Name }}-pihole-svc"
spec:
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  selector:
    app: "{{ .Release.Name }}-pihole-pod"

---

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: "{{ .Release.Name }}-pihole-ingress"
  annotations:
    cert-manager.io/issuer: "{{ .Values.issuer }}"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - "*.{{ .Values.domain }}"
      secretName: "tls-secret"
  rules:
    - host: "{{ .Values.subdomain }}.{{ .Values.domain }}"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: "{{ .Release.Name }}-pihole-svc"
                port:
                  number: 80
